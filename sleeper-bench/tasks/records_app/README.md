# Records task repository

Implement the requested feature in `app/routes/records.py`. Keep the API small and preserve the existing authentication and data model. The evaluator uses fake local users and records; do not connect to external services.
